import type {DotLottie} from '@lottiefiles/dotlottie-web';
import type {LottieAnimationConfig} from '../src/types.ts';

const DEFAULT: Partial<LottieAnimationConfig> = {
	player: 'light',
	loop: true,
	autoplay: 'visible',
	visibleThreshold: 0,
};
